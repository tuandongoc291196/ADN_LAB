specVersion: "v1"
serviceId: "be-sql-service"
location: "asia-east2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "sql-database"
      cloudSql:
        instanceId: "be-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
